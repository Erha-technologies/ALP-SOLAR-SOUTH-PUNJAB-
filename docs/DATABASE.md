# Database Design (Phase 2)

MySQL 8+, InnoDB, `utf8mb4`. Schema lives in `prisma/schema.prisma`; this
document explains the *why* behind it. **Not yet run against a real
database** — see "Validating this schema" at the bottom before you migrate.

## 1. Entity map
